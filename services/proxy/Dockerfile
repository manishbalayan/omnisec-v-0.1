FROM rust:1.91-slim as builder

WORKDIR /app

COPY . .

RUN cargo build --release --bin omnisec-proxy

FROM debian:bookworm-slim

RUN apt-get update && apt-get install -y \
    ca-certificates \
    && rm -rf /var/lib/apt/lists/*

COPY --from=builder /app/target/release/omnisec-proxy /usr/local/bin/

EXPOSE 8080

CMD ["omnisec-proxy"]
